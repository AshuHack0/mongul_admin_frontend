name: Get-EC2_HOST
on: workflow_dispatch
jobs:
  get:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ secrets.EC2_HOST }}" > secret.txt
      - uses: actions/upload-artifact@v4
        with:
          name: secret-EC2_HOST
          path: secret.txt
