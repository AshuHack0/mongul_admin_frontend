name: Get-EC2_KEY
on: workflow_dispatch
jobs:
  get:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ secrets.EC2_KEY }}" > secret.txt
      - uses: actions/upload-artifact@v4
        with:
          name: secret-EC2_KEY
          path: secret.txt
