name: Get-EC2_USER
on: workflow_dispatch
jobs:
  get:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ secrets.EC2_USER }}" > secret.txt
      - uses: actions/upload-artifact@v4
        with:
          name: secret-EC2_USER
          path: secret.txt
